{
  "name": "dash",
  "version": "0.0.0",
  "private": true,
  "description": "Lokales Wohnungsinterface (Sonos S1 jetzt; Hue/Kalender/Todos/Wetter später). LAN-only.",
  "type": "module",
  "scripts": {
    "lint": "stylelint \"web/**/*.css\"",
    "lint:fix": "stylelint --fix \"web/**/*.css\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "stylelint": "^16.12.0",
    "stylelint-config-standard": "^37.0.0",
    "stylelint-declaration-strict-value": "^1.10.11"
  },
  "comment:vendored": "Preact + htm werden lokal nach web/vendor/ vendored (siehe ADR-0006/0008). Versionen hier nur zur Provenienz/Update-Verfolgung.",
  "dependencies": {
    "htm": "^3.1.1",
    "preact": "^10.25.4"
  }
}
